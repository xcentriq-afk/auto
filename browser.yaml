name: xclab

services:
  filebrowser:
    image: hurlenko/filebrowser
    user: "${UID}:${GID}"
    ports:
      - 8082:8080
    volumes:
      - /media/usb3:/data
      - /home/xc/auto/config/browser:/config
    environment:
      - FB_BASEURL=/filebrowser
    restart: always

